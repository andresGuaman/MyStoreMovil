<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.agmr.mystore">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".Carrito_Detalle"></activity>
        <activity android:name=".DescripcionProducto" />
        <activity android:name=".CrearProducto" />
        <activity android:name=".MenuProductos" />
        <activity android:name=".Lista_pedidos" />
        <activity android:name=".MenuAdministrador" />
        <activity android:name=".MenuPrincipal" />
        <activity android:name=".RegistroUsuario" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>